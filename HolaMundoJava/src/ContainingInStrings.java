public class ContainingInStrings {
    public static void main(String[] args) {
        String str1 = "The idea is to save all the data";

        // To see if in the string exists any word in it, we can use the method contains
        System.out.println(str1.contains("data"));
    }
}
